services:
  - type: web
    name: pc-builder-backend
    env: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: PORT
        value: 10000
    healthCheckPath: /api/health

